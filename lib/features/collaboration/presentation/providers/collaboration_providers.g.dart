// GENERATED CODE - DO NOT MODIFY BY HAND

part of 'collaboration_providers.dart';

// **************************************************************************
// RiverpodGenerator
// **************************************************************************

// GENERATED CODE - DO NOT MODIFY BY HAND
// ignore_for_file: type=lint, type=warning

@ProviderFor(collaborationRemoteDataSource)
final collaborationRemoteDataSourceProvider =
    CollaborationRemoteDataSourceProvider._();

final class CollaborationRemoteDataSourceProvider
    extends
        $FunctionalProvider<
          CollaborationRemoteDataSource,
          CollaborationRemoteDataSource,
          CollaborationRemoteDataSource
        >
    with $Provider<CollaborationRemoteDataSource> {
  CollaborationRemoteDataSourceProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'collaborationRemoteDataSourceProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$collaborationRemoteDataSourceHash();

  @$internal
  @override
  $ProviderElement<CollaborationRemoteDataSource> $createElement(
    $ProviderPointer pointer,
  ) => $ProviderElement(pointer);

  @override
  CollaborationRemoteDataSource create(Ref ref) {
    return collaborationRemoteDataSource(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(CollaborationRemoteDataSource value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<CollaborationRemoteDataSource>(
        value,
      ),
    );
  }
}

String _$collaborationRemoteDataSourceHash() =>
    r'e6944c423536130268f8e6a3449122c28a1bf589';

@ProviderFor(publishingRemoteDataSource)
final publishingRemoteDataSourceProvider =
    PublishingRemoteDataSourceProvider._();

final class PublishingRemoteDataSourceProvider
    extends
        $FunctionalProvider<
          PublishingRemoteDataSource,
          PublishingRemoteDataSource,
          PublishingRemoteDataSource
        >
    with $Provider<PublishingRemoteDataSource> {
  PublishingRemoteDataSourceProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'publishingRemoteDataSourceProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$publishingRemoteDataSourceHash();

  @$internal
  @override
  $ProviderElement<PublishingRemoteDataSource> $createElement(
    $ProviderPointer pointer,
  ) => $ProviderElement(pointer);

  @override
  PublishingRemoteDataSource create(Ref ref) {
    return publishingRemoteDataSource(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(PublishingRemoteDataSource value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<PublishingRemoteDataSource>(value),
    );
  }
}

String _$publishingRemoteDataSourceHash() =>
    r'df0846967f580d9ee6d617831ecfd7a0d954ebb6';

@ProviderFor(trustRemoteDataSource)
final trustRemoteDataSourceProvider = TrustRemoteDataSourceProvider._();

final class TrustRemoteDataSourceProvider
    extends
        $FunctionalProvider<
          TrustRemoteDataSource,
          TrustRemoteDataSource,
          TrustRemoteDataSource
        >
    with $Provider<TrustRemoteDataSource> {
  TrustRemoteDataSourceProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'trustRemoteDataSourceProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$trustRemoteDataSourceHash();

  @$internal
  @override
  $ProviderElement<TrustRemoteDataSource> $createElement(
    $ProviderPointer pointer,
  ) => $ProviderElement(pointer);

  @override
  TrustRemoteDataSource create(Ref ref) {
    return trustRemoteDataSource(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(TrustRemoteDataSource value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<TrustRemoteDataSource>(value),
    );
  }
}

String _$trustRemoteDataSourceHash() =>
    r'1250b124ea367d1d3f1f3835aecaf329efb07386';

@ProviderFor(collaborationRepository)
final collaborationRepositoryProvider = CollaborationRepositoryProvider._();

final class CollaborationRepositoryProvider
    extends
        $FunctionalProvider<
          CollaborationRepository,
          CollaborationRepository,
          CollaborationRepository
        >
    with $Provider<CollaborationRepository> {
  CollaborationRepositoryProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'collaborationRepositoryProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$collaborationRepositoryHash();

  @$internal
  @override
  $ProviderElement<CollaborationRepository> $createElement(
    $ProviderPointer pointer,
  ) => $ProviderElement(pointer);

  @override
  CollaborationRepository create(Ref ref) {
    return collaborationRepository(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(CollaborationRepository value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<CollaborationRepository>(value),
    );
  }
}

String _$collaborationRepositoryHash() =>
    r'8590623cdecc5b27bf6ac07349cb72ed5d481e65';

@ProviderFor(publishingRepository)
final publishingRepositoryProvider = PublishingRepositoryProvider._();

final class PublishingRepositoryProvider
    extends
        $FunctionalProvider<
          PublishingRepository,
          PublishingRepository,
          PublishingRepository
        >
    with $Provider<PublishingRepository> {
  PublishingRepositoryProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'publishingRepositoryProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$publishingRepositoryHash();

  @$internal
  @override
  $ProviderElement<PublishingRepository> $createElement(
    $ProviderPointer pointer,
  ) => $ProviderElement(pointer);

  @override
  PublishingRepository create(Ref ref) {
    return publishingRepository(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(PublishingRepository value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<PublishingRepository>(value),
    );
  }
}

String _$publishingRepositoryHash() =>
    r'07e35cee9193654dc9ee8678075b64bfdac1d382';

@ProviderFor(trustRepository)
final trustRepositoryProvider = TrustRepositoryProvider._();

final class TrustRepositoryProvider
    extends
        $FunctionalProvider<TrustRepository, TrustRepository, TrustRepository>
    with $Provider<TrustRepository> {
  TrustRepositoryProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'trustRepositoryProvider',
        isAutoDispose: false,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$trustRepositoryHash();

  @$internal
  @override
  $ProviderElement<TrustRepository> $createElement($ProviderPointer pointer) =>
      $ProviderElement(pointer);

  @override
  TrustRepository create(Ref ref) {
    return trustRepository(ref);
  }

  /// {@macro riverpod.override_with_value}
  Override overrideWithValue(TrustRepository value) {
    return $ProviderOverride(
      origin: this,
      providerOverride: $SyncValueProvider<TrustRepository>(value),
    );
  }
}

String _$trustRepositoryHash() => r'9acd81a366742c0b79ee53107c5671a3dfdf55c9';

/// The collaborators on a story.

@ProviderFor(storyMembers)
final storyMembersProvider = StoryMembersFamily._();

/// The collaborators on a story.

final class StoryMembersProvider
    extends
        $FunctionalProvider<
          AsyncValue<List<StoryMember>>,
          List<StoryMember>,
          FutureOr<List<StoryMember>>
        >
    with
        $FutureModifier<List<StoryMember>>,
        $FutureProvider<List<StoryMember>> {
  /// The collaborators on a story.
  StoryMembersProvider._({
    required StoryMembersFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storyMembersProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storyMembersHash();

  @override
  String toString() {
    return r'storyMembersProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<List<StoryMember>> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<List<StoryMember>> create(Ref ref) {
    final argument = this.argument as String;
    return storyMembers(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StoryMembersProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storyMembersHash() => r'7abff14464f6f4e31fcf16b5a32dceb2a663bef7';

/// The collaborators on a story.

final class StoryMembersFamily extends $Family
    with $FunctionalFamilyOverride<FutureOr<List<StoryMember>>, String> {
  StoryMembersFamily._()
    : super(
        retry: null,
        name: r'storyMembersProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// The collaborators on a story.

  StoryMembersProvider call(String storyId) =>
      StoryMembersProvider._(argument: storyId, from: this);

  @override
  String toString() => r'storyMembersProvider';
}

/// The server-authoritative capability map — the SINGLE thing collaboration UI gates
/// affordances on. Never throws: on failure it falls back to a fail-closed read-only
/// map so gating always resolves (the policy engine re-checks anyway).

@ProviderFor(storyCapabilities)
final storyCapabilitiesProvider = StoryCapabilitiesFamily._();

/// The server-authoritative capability map — the SINGLE thing collaboration UI gates
/// affordances on. Never throws: on failure it falls back to a fail-closed read-only
/// map so gating always resolves (the policy engine re-checks anyway).

final class StoryCapabilitiesProvider
    extends
        $FunctionalProvider<
          AsyncValue<StoryCapabilities>,
          StoryCapabilities,
          FutureOr<StoryCapabilities>
        >
    with
        $FutureModifier<StoryCapabilities>,
        $FutureProvider<StoryCapabilities> {
  /// The server-authoritative capability map — the SINGLE thing collaboration UI gates
  /// affordances on. Never throws: on failure it falls back to a fail-closed read-only
  /// map so gating always resolves (the policy engine re-checks anyway).
  StoryCapabilitiesProvider._({
    required StoryCapabilitiesFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storyCapabilitiesProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storyCapabilitiesHash();

  @override
  String toString() {
    return r'storyCapabilitiesProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<StoryCapabilities> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<StoryCapabilities> create(Ref ref) {
    final argument = this.argument as String;
    return storyCapabilities(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StoryCapabilitiesProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storyCapabilitiesHash() => r'a62461cbf76704833d0a94805f3e9846556d0635';

/// The server-authoritative capability map — the SINGLE thing collaboration UI gates
/// affordances on. Never throws: on failure it falls back to a fail-closed read-only
/// map so gating always resolves (the policy engine re-checks anyway).

final class StoryCapabilitiesFamily extends $Family
    with $FunctionalFamilyOverride<FutureOr<StoryCapabilities>, String> {
  StoryCapabilitiesFamily._()
    : super(
        retry: null,
        name: r'storyCapabilitiesProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// The server-authoritative capability map — the SINGLE thing collaboration UI gates
  /// affordances on. Never throws: on failure it falls back to a fail-closed read-only
  /// map so gating always resolves (the policy engine re-checks anyway).

  StoryCapabilitiesProvider call(String storyId) =>
      StoryCapabilitiesProvider._(argument: storyId, from: this);

  @override
  String toString() => r'storyCapabilitiesProvider';
}

/// The story's collaborator seat allowance (B6, `platfrom/docs/45` §4.11) — used / limit /
/// remaining, charged to the story OWNER's plan.
///
/// **Never throws.** The route is `story.invite`-authorized, so a reader gets a 403 and a
/// story whose allowance cannot be read would otherwise take the collaborators screen down
/// with it. It falls back to [CollaboratorLimit.unknown], which the screen reads as "no
/// number to show" rather than as a refusal: the invite control stays live unless the
/// server actually said there is no seat. Losing an upsell is a missed sale; hiding the
/// only management action on the screen is a broken app.

@ProviderFor(storyCollaboratorLimit)
final storyCollaboratorLimitProvider = StoryCollaboratorLimitFamily._();

/// The story's collaborator seat allowance (B6, `platfrom/docs/45` §4.11) — used / limit /
/// remaining, charged to the story OWNER's plan.
///
/// **Never throws.** The route is `story.invite`-authorized, so a reader gets a 403 and a
/// story whose allowance cannot be read would otherwise take the collaborators screen down
/// with it. It falls back to [CollaboratorLimit.unknown], which the screen reads as "no
/// number to show" rather than as a refusal: the invite control stays live unless the
/// server actually said there is no seat. Losing an upsell is a missed sale; hiding the
/// only management action on the screen is a broken app.

final class StoryCollaboratorLimitProvider
    extends
        $FunctionalProvider<
          AsyncValue<CollaboratorLimit>,
          CollaboratorLimit,
          FutureOr<CollaboratorLimit>
        >
    with
        $FutureModifier<CollaboratorLimit>,
        $FutureProvider<CollaboratorLimit> {
  /// The story's collaborator seat allowance (B6, `platfrom/docs/45` §4.11) — used / limit /
  /// remaining, charged to the story OWNER's plan.
  ///
  /// **Never throws.** The route is `story.invite`-authorized, so a reader gets a 403 and a
  /// story whose allowance cannot be read would otherwise take the collaborators screen down
  /// with it. It falls back to [CollaboratorLimit.unknown], which the screen reads as "no
  /// number to show" rather than as a refusal: the invite control stays live unless the
  /// server actually said there is no seat. Losing an upsell is a missed sale; hiding the
  /// only management action on the screen is a broken app.
  StoryCollaboratorLimitProvider._({
    required StoryCollaboratorLimitFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storyCollaboratorLimitProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storyCollaboratorLimitHash();

  @override
  String toString() {
    return r'storyCollaboratorLimitProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<CollaboratorLimit> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<CollaboratorLimit> create(Ref ref) {
    final argument = this.argument as String;
    return storyCollaboratorLimit(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StoryCollaboratorLimitProvider &&
        other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storyCollaboratorLimitHash() =>
    r'e929d2099cca1edee3fc1171d674cd0bdcb720a4';

/// The story's collaborator seat allowance (B6, `platfrom/docs/45` §4.11) — used / limit /
/// remaining, charged to the story OWNER's plan.
///
/// **Never throws.** The route is `story.invite`-authorized, so a reader gets a 403 and a
/// story whose allowance cannot be read would otherwise take the collaborators screen down
/// with it. It falls back to [CollaboratorLimit.unknown], which the screen reads as "no
/// number to show" rather than as a refusal: the invite control stays live unless the
/// server actually said there is no seat. Losing an upsell is a missed sale; hiding the
/// only management action on the screen is a broken app.

final class StoryCollaboratorLimitFamily extends $Family
    with $FunctionalFamilyOverride<FutureOr<CollaboratorLimit>, String> {
  StoryCollaboratorLimitFamily._()
    : super(
        retry: null,
        name: r'storyCollaboratorLimitProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// The story's collaborator seat allowance (B6, `platfrom/docs/45` §4.11) — used / limit /
  /// remaining, charged to the story OWNER's plan.
  ///
  /// **Never throws.** The route is `story.invite`-authorized, so a reader gets a 403 and a
  /// story whose allowance cannot be read would otherwise take the collaborators screen down
  /// with it. It falls back to [CollaboratorLimit.unknown], which the screen reads as "no
  /// number to show" rather than as a refusal: the invite control stays live unless the
  /// server actually said there is no seat. Losing an upsell is a missed sale; hiding the
  /// only management action on the screen is a broken app.

  StoryCollaboratorLimitProvider call(String storyId) =>
      StoryCollaboratorLimitProvider._(argument: storyId, from: this);

  @override
  String toString() => r'storyCollaboratorLimitProvider';
}

/// The first page of root comments on a story. The endpoint is cursor-paginated
/// (C-10); this provider exposes the first page and [storyCommentThread] fetches a
/// thread's replies on demand (C-5).

@ProviderFor(storyComments)
final storyCommentsProvider = StoryCommentsFamily._();

/// The first page of root comments on a story. The endpoint is cursor-paginated
/// (C-10); this provider exposes the first page and [storyCommentThread] fetches a
/// thread's replies on demand (C-5).

final class StoryCommentsProvider
    extends
        $FunctionalProvider<
          AsyncValue<CursorPage<CollaborationComment>>,
          CursorPage<CollaborationComment>,
          FutureOr<CursorPage<CollaborationComment>>
        >
    with
        $FutureModifier<CursorPage<CollaborationComment>>,
        $FutureProvider<CursorPage<CollaborationComment>> {
  /// The first page of root comments on a story. The endpoint is cursor-paginated
  /// (C-10); this provider exposes the first page and [storyCommentThread] fetches a
  /// thread's replies on demand (C-5).
  StoryCommentsProvider._({
    required StoryCommentsFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storyCommentsProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storyCommentsHash();

  @override
  String toString() {
    return r'storyCommentsProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<CursorPage<CollaborationComment>> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<CursorPage<CollaborationComment>> create(Ref ref) {
    final argument = this.argument as String;
    return storyComments(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StoryCommentsProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storyCommentsHash() => r'92f5c08fa34fe0ef0aff3f23b394aaab308354e6';

/// The first page of root comments on a story. The endpoint is cursor-paginated
/// (C-10); this provider exposes the first page and [storyCommentThread] fetches a
/// thread's replies on demand (C-5).

final class StoryCommentsFamily extends $Family
    with
        $FunctionalFamilyOverride<
          FutureOr<CursorPage<CollaborationComment>>,
          String
        > {
  StoryCommentsFamily._()
    : super(
        retry: null,
        name: r'storyCommentsProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// The first page of root comments on a story. The endpoint is cursor-paginated
  /// (C-10); this provider exposes the first page and [storyCommentThread] fetches a
  /// thread's replies on demand (C-5).

  StoryCommentsProvider call(String storyId) =>
      StoryCommentsProvider._(argument: storyId, from: this);

  @override
  String toString() => r'storyCommentsProvider';
}

/// A comment's replies (`GET /comments/:id/thread`). `CommentDto` carries no
/// `replies`, so a thread is a separate read.

@ProviderFor(storyCommentThread)
final storyCommentThreadProvider = StoryCommentThreadFamily._();

/// A comment's replies (`GET /comments/:id/thread`). `CommentDto` carries no
/// `replies`, so a thread is a separate read.

final class StoryCommentThreadProvider
    extends
        $FunctionalProvider<
          AsyncValue<CommentThread>,
          CommentThread,
          FutureOr<CommentThread>
        >
    with $FutureModifier<CommentThread>, $FutureProvider<CommentThread> {
  /// A comment's replies (`GET /comments/:id/thread`). `CommentDto` carries no
  /// `replies`, so a thread is a separate read.
  StoryCommentThreadProvider._({
    required StoryCommentThreadFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storyCommentThreadProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storyCommentThreadHash();

  @override
  String toString() {
    return r'storyCommentThreadProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<CommentThread> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<CommentThread> create(Ref ref) {
    final argument = this.argument as String;
    return storyCommentThread(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StoryCommentThreadProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storyCommentThreadHash() =>
    r'9b9f6eb9b577e8686c86f8b2b38580d6e749f8b0';

/// A comment's replies (`GET /comments/:id/thread`). `CommentDto` carries no
/// `replies`, so a thread is a separate read.

final class StoryCommentThreadFamily extends $Family
    with $FunctionalFamilyOverride<FutureOr<CommentThread>, String> {
  StoryCommentThreadFamily._()
    : super(
        retry: null,
        name: r'storyCommentThreadProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// A comment's replies (`GET /comments/:id/thread`). `CommentDto` carries no
  /// `replies`, so a thread is a separate read.

  StoryCommentThreadProvider call(String commentId) =>
      StoryCommentThreadProvider._(argument: commentId, from: this);

  @override
  String toString() => r'storyCommentThreadProvider';
}

/// The first page of edit suggestions on a story (cursor-paginated, C-10).

@ProviderFor(storySuggestions)
final storySuggestionsProvider = StorySuggestionsFamily._();

/// The first page of edit suggestions on a story (cursor-paginated, C-10).

final class StorySuggestionsProvider
    extends
        $FunctionalProvider<
          AsyncValue<CursorPage<EditSuggestion>>,
          CursorPage<EditSuggestion>,
          FutureOr<CursorPage<EditSuggestion>>
        >
    with
        $FutureModifier<CursorPage<EditSuggestion>>,
        $FutureProvider<CursorPage<EditSuggestion>> {
  /// The first page of edit suggestions on a story (cursor-paginated, C-10).
  StorySuggestionsProvider._({
    required StorySuggestionsFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storySuggestionsProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storySuggestionsHash();

  @override
  String toString() {
    return r'storySuggestionsProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<CursorPage<EditSuggestion>> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<CursorPage<EditSuggestion>> create(Ref ref) {
    final argument = this.argument as String;
    return storySuggestions(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StorySuggestionsProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storySuggestionsHash() => r'0abbaa008750e3014ba516fe7082de87328a57a6';

/// The first page of edit suggestions on a story (cursor-paginated, C-10).

final class StorySuggestionsFamily extends $Family
    with
        $FunctionalFamilyOverride<
          FutureOr<CursorPage<EditSuggestion>>,
          String
        > {
  StorySuggestionsFamily._()
    : super(
        retry: null,
        name: r'storySuggestionsProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// The first page of edit suggestions on a story (cursor-paginated, C-10).

  StorySuggestionsProvider call(String storyId) =>
      StorySuggestionsProvider._(argument: storyId, from: this);

  @override
  String toString() => r'storySuggestionsProvider';
}

/// The outstanding invitations issued for a story.

@ProviderFor(storyInvitations)
final storyInvitationsProvider = StoryInvitationsFamily._();

/// The outstanding invitations issued for a story.

final class StoryInvitationsProvider
    extends
        $FunctionalProvider<
          AsyncValue<List<StoryInvitation>>,
          List<StoryInvitation>,
          FutureOr<List<StoryInvitation>>
        >
    with
        $FutureModifier<List<StoryInvitation>>,
        $FutureProvider<List<StoryInvitation>> {
  /// The outstanding invitations issued for a story.
  StoryInvitationsProvider._({
    required StoryInvitationsFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storyInvitationsProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storyInvitationsHash();

  @override
  String toString() {
    return r'storyInvitationsProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<List<StoryInvitation>> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<List<StoryInvitation>> create(Ref ref) {
    final argument = this.argument as String;
    return storyInvitations(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StoryInvitationsProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storyInvitationsHash() => r'c217372e2546684408a666d9fea68f93cbae0d27';

/// The outstanding invitations issued for a story.

final class StoryInvitationsFamily extends $Family
    with $FunctionalFamilyOverride<FutureOr<List<StoryInvitation>>, String> {
  StoryInvitationsFamily._()
    : super(
        retry: null,
        name: r'storyInvitationsProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// The outstanding invitations issued for a story.

  StoryInvitationsProvider call(String storyId) =>
      StoryInvitationsProvider._(argument: storyId, from: this);

  @override
  String toString() => r'storyInvitationsProvider';
}

/// The live presence roster on a story (drives the presence bar).

@ProviderFor(storyPresence)
final storyPresenceProvider = StoryPresenceFamily._();

/// The live presence roster on a story (drives the presence bar).

final class StoryPresenceProvider
    extends
        $FunctionalProvider<
          AsyncValue<List<PresenceEntry>>,
          List<PresenceEntry>,
          FutureOr<List<PresenceEntry>>
        >
    with
        $FutureModifier<List<PresenceEntry>>,
        $FutureProvider<List<PresenceEntry>> {
  /// The live presence roster on a story (drives the presence bar).
  StoryPresenceProvider._({
    required StoryPresenceFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storyPresenceProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storyPresenceHash();

  @override
  String toString() {
    return r'storyPresenceProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<List<PresenceEntry>> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<List<PresenceEntry>> create(Ref ref) {
    final argument = this.argument as String;
    return storyPresence(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StoryPresenceProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storyPresenceHash() => r'456f99dcba1e9d5db9a62b22dded9eb6cc89e6d9';

/// The live presence roster on a story (drives the presence bar).

final class StoryPresenceFamily extends $Family
    with $FunctionalFamilyOverride<FutureOr<List<PresenceEntry>>, String> {
  StoryPresenceFamily._()
    : super(
        retry: null,
        name: r'storyPresenceProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// The live presence roster on a story (drives the presence bar).

  StoryPresenceProvider call(String storyId) =>
      StoryPresenceProvider._(argument: storyId, from: this);

  @override
  String toString() => r'storyPresenceProvider';
}

/// The collaboration audit feed for a story.

@ProviderFor(storyActivity)
final storyActivityProvider = StoryActivityFamily._();

/// The collaboration audit feed for a story.

final class StoryActivityProvider
    extends
        $FunctionalProvider<
          AsyncValue<CursorPage<CollaborationActivityEntry>>,
          CursorPage<CollaborationActivityEntry>,
          FutureOr<CursorPage<CollaborationActivityEntry>>
        >
    with
        $FutureModifier<CursorPage<CollaborationActivityEntry>>,
        $FutureProvider<CursorPage<CollaborationActivityEntry>> {
  /// The collaboration audit feed for a story.
  StoryActivityProvider._({
    required StoryActivityFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storyActivityProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storyActivityHash();

  @override
  String toString() {
    return r'storyActivityProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<CursorPage<CollaborationActivityEntry>> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<CursorPage<CollaborationActivityEntry>> create(Ref ref) {
    final argument = this.argument as String;
    return storyActivity(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StoryActivityProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storyActivityHash() => r'2dd2304326c40d3cc7fba27b89cc7459cfb9b34d';

/// The collaboration audit feed for a story.

final class StoryActivityFamily extends $Family
    with
        $FunctionalFamilyOverride<
          FutureOr<CursorPage<CollaborationActivityEntry>>,
          String
        > {
  StoryActivityFamily._()
    : super(
        retry: null,
        name: r'storyActivityProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// The collaboration audit feed for a story.

  StoryActivityProvider call(String storyId) =>
      StoryActivityProvider._(argument: storyId, from: this);

  @override
  String toString() => r'storyActivityProvider';
}

/// The current user's inbound invitation inbox (`GET /me/invitations`).

@ProviderFor(myInvitations)
final myInvitationsProvider = MyInvitationsProvider._();

/// The current user's inbound invitation inbox (`GET /me/invitations`).

final class MyInvitationsProvider
    extends
        $FunctionalProvider<
          AsyncValue<List<StoryInvitation>>,
          List<StoryInvitation>,
          FutureOr<List<StoryInvitation>>
        >
    with
        $FutureModifier<List<StoryInvitation>>,
        $FutureProvider<List<StoryInvitation>> {
  /// The current user's inbound invitation inbox (`GET /me/invitations`).
  MyInvitationsProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'myInvitationsProvider',
        isAutoDispose: true,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$myInvitationsHash();

  @$internal
  @override
  $FutureProviderElement<List<StoryInvitation>> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<List<StoryInvitation>> create(Ref ref) {
    return myInvitations(ref);
  }
}

String _$myInvitationsHash() => r'e7764504e4db57d009cdb3bdc0f458aebc1f6eee';

/// The viewer's own user id, or null if it cannot be resolved. Used only for
/// self-service affordances the capability map cannot express (C-12); never for an
/// authorization decision — the server re-checks every action.

@ProviderFor(viewerId)
final viewerIdProvider = ViewerIdProvider._();

/// The viewer's own user id, or null if it cannot be resolved. Used only for
/// self-service affordances the capability map cannot express (C-12); never for an
/// authorization decision — the server re-checks every action.

final class ViewerIdProvider
    extends $FunctionalProvider<AsyncValue<String?>, String?, FutureOr<String?>>
    with $FutureModifier<String?>, $FutureProvider<String?> {
  /// The viewer's own user id, or null if it cannot be resolved. Used only for
  /// self-service affordances the capability map cannot express (C-12); never for an
  /// authorization decision — the server re-checks every action.
  ViewerIdProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'viewerIdProvider',
        isAutoDispose: true,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$viewerIdHash();

  @$internal
  @override
  $FutureProviderElement<String?> $createElement($ProviderPointer pointer) =>
      $FutureProviderElement(pointer);

  @override
  FutureOr<String?> create(Ref ref) {
    return viewerId(ref);
  }
}

String _$viewerIdHash() => r'3613abdd038e74ef1094b26337e7fc6a6e80b96c';

/// The review session for a story, or null when review has never been requested —
/// which the endpoint expresses as `200 {data: null}`, not a 404.
///
/// This used to map `NOT_FOUND → null`, a code the endpoint never returns. The real
/// null-data response became `API_MALFORMED_RESPONSE` inside `ApiClient.get` and was
/// rethrown here, so the Review card showed an error for every story that had never
/// been submitted — the default state — and its `review == null` branch was dead
/// code. The nullability now comes from the data source via `getOrNull`
/// (defect **P-4**, `docs/56` §2.2).

@ProviderFor(storyReview)
final storyReviewProvider = StoryReviewFamily._();

/// The review session for a story, or null when review has never been requested —
/// which the endpoint expresses as `200 {data: null}`, not a 404.
///
/// This used to map `NOT_FOUND → null`, a code the endpoint never returns. The real
/// null-data response became `API_MALFORMED_RESPONSE` inside `ApiClient.get` and was
/// rethrown here, so the Review card showed an error for every story that had never
/// been submitted — the default state — and its `review == null` branch was dead
/// code. The nullability now comes from the data source via `getOrNull`
/// (defect **P-4**, `docs/56` §2.2).

final class StoryReviewProvider
    extends
        $FunctionalProvider<
          AsyncValue<ReviewSession?>,
          ReviewSession?,
          FutureOr<ReviewSession?>
        >
    with $FutureModifier<ReviewSession?>, $FutureProvider<ReviewSession?> {
  /// The review session for a story, or null when review has never been requested —
  /// which the endpoint expresses as `200 {data: null}`, not a 404.
  ///
  /// This used to map `NOT_FOUND → null`, a code the endpoint never returns. The real
  /// null-data response became `API_MALFORMED_RESPONSE` inside `ApiClient.get` and was
  /// rethrown here, so the Review card showed an error for every story that had never
  /// been submitted — the default state — and its `review == null` branch was dead
  /// code. The nullability now comes from the data source via `getOrNull`
  /// (defect **P-4**, `docs/56` §2.2).
  StoryReviewProvider._({
    required StoryReviewFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storyReviewProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storyReviewHash();

  @override
  String toString() {
    return r'storyReviewProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<ReviewSession?> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<ReviewSession?> create(Ref ref) {
    final argument = this.argument as String;
    return storyReview(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StoryReviewProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storyReviewHash() => r'6d82100138ddb755a0888ac3a544c076562d0e87';

/// The review session for a story, or null when review has never been requested —
/// which the endpoint expresses as `200 {data: null}`, not a 404.
///
/// This used to map `NOT_FOUND → null`, a code the endpoint never returns. The real
/// null-data response became `API_MALFORMED_RESPONSE` inside `ApiClient.get` and was
/// rethrown here, so the Review card showed an error for every story that had never
/// been submitted — the default state — and its `review == null` branch was dead
/// code. The nullability now comes from the data source via `getOrNull`
/// (defect **P-4**, `docs/56` §2.2).

final class StoryReviewFamily extends $Family
    with $FunctionalFamilyOverride<FutureOr<ReviewSession?>, String> {
  StoryReviewFamily._()
    : super(
        retry: null,
        name: r'storyReviewProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// The review session for a story, or null when review has never been requested —
  /// which the endpoint expresses as `200 {data: null}`, not a 404.
  ///
  /// This used to map `NOT_FOUND → null`, a code the endpoint never returns. The real
  /// null-data response became `API_MALFORMED_RESPONSE` inside `ApiClient.get` and was
  /// rethrown here, so the Review card showed an error for every story that had never
  /// been submitted — the default state — and its `review == null` branch was dead
  /// code. The nullability now comes from the data source via `getOrNull`
  /// (defect **P-4**, `docs/56` §2.2).

  StoryReviewProvider call(String storyId) =>
      StoryReviewProvider._(argument: storyId, from: this);

  @override
  String toString() => r'storyReviewProvider';
}

/// A story's version history — the versions the OWNER's plan shows, newest first, plus
/// the true total of everything stored (B7, `platfrom/docs/45` §4.12).
///
/// Read the count from `total`, never from `items.length`: the latter is the clamped
/// number and would report a thirty-two-version story as having five.

@ProviderFor(storySnapshots)
final storySnapshotsProvider = StorySnapshotsFamily._();

/// A story's version history — the versions the OWNER's plan shows, newest first, plus
/// the true total of everything stored (B7, `platfrom/docs/45` §4.12).
///
/// Read the count from `total`, never from `items.length`: the latter is the clamped
/// number and would report a thirty-two-version story as having five.

final class StorySnapshotsProvider
    extends
        $FunctionalProvider<
          AsyncValue<StorySnapshotHistory>,
          StorySnapshotHistory,
          FutureOr<StorySnapshotHistory>
        >
    with
        $FutureModifier<StorySnapshotHistory>,
        $FutureProvider<StorySnapshotHistory> {
  /// A story's version history — the versions the OWNER's plan shows, newest first, plus
  /// the true total of everything stored (B7, `platfrom/docs/45` §4.12).
  ///
  /// Read the count from `total`, never from `items.length`: the latter is the clamped
  /// number and would report a thirty-two-version story as having five.
  StorySnapshotsProvider._({
    required StorySnapshotsFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'storySnapshotsProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$storySnapshotsHash();

  @override
  String toString() {
    return r'storySnapshotsProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<StorySnapshotHistory> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<StorySnapshotHistory> create(Ref ref) {
    final argument = this.argument as String;
    return storySnapshots(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is StorySnapshotsProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$storySnapshotsHash() => r'2c7110f8af971f1650b12a6c57e32576c88e7adf';

/// A story's version history — the versions the OWNER's plan shows, newest first, plus
/// the true total of everything stored (B7, `platfrom/docs/45` §4.12).
///
/// Read the count from `total`, never from `items.length`: the latter is the clamped
/// number and would report a thirty-two-version story as having five.

final class StorySnapshotsFamily extends $Family
    with $FunctionalFamilyOverride<FutureOr<StorySnapshotHistory>, String> {
  StorySnapshotsFamily._()
    : super(
        retry: null,
        name: r'storySnapshotsProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// A story's version history — the versions the OWNER's plan shows, newest first, plus
  /// the true total of everything stored (B7, `platfrom/docs/45` §4.12).
  ///
  /// Read the count from `total`, never from `items.length`: the latter is the clamped
  /// number and would report a thirty-two-version story as having five.

  StorySnapshotsProvider call(String storyId) =>
      StorySnapshotsProvider._(argument: storyId, from: this);

  @override
  String toString() => r'storySnapshotsProvider';
}

/// The publication history for a story.

@ProviderFor(publicationHistory)
final publicationHistoryProvider = PublicationHistoryFamily._();

/// The publication history for a story.

final class PublicationHistoryProvider
    extends
        $FunctionalProvider<
          AsyncValue<List<PublicationEvent>>,
          List<PublicationEvent>,
          FutureOr<List<PublicationEvent>>
        >
    with
        $FutureModifier<List<PublicationEvent>>,
        $FutureProvider<List<PublicationEvent>> {
  /// The publication history for a story.
  PublicationHistoryProvider._({
    required PublicationHistoryFamily super.from,
    required String super.argument,
  }) : super(
         retry: null,
         name: r'publicationHistoryProvider',
         isAutoDispose: true,
         dependencies: null,
         $allTransitiveDependencies: null,
       );

  @override
  String debugGetCreateSourceHash() => _$publicationHistoryHash();

  @override
  String toString() {
    return r'publicationHistoryProvider'
        ''
        '($argument)';
  }

  @$internal
  @override
  $FutureProviderElement<List<PublicationEvent>> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<List<PublicationEvent>> create(Ref ref) {
    final argument = this.argument as String;
    return publicationHistory(ref, argument);
  }

  @override
  bool operator ==(Object other) {
    return other is PublicationHistoryProvider && other.argument == argument;
  }

  @override
  int get hashCode {
    return argument.hashCode;
  }
}

String _$publicationHistoryHash() =>
    r'4691a5954bc1f04c25376f1ef25e597049b9b7cf';

/// The publication history for a story.

final class PublicationHistoryFamily extends $Family
    with $FunctionalFamilyOverride<FutureOr<List<PublicationEvent>>, String> {
  PublicationHistoryFamily._()
    : super(
        retry: null,
        name: r'publicationHistoryProvider',
        dependencies: null,
        $allTransitiveDependencies: null,
        isAutoDispose: true,
      );

  /// The publication history for a story.

  PublicationHistoryProvider call(String storyId) =>
      PublicationHistoryProvider._(argument: storyId, from: this);

  @override
  String toString() => r'publicationHistoryProvider';
}

/// The current user's trust standing (the restricted-state screens read this).

@ProviderFor(trustSummary)
final trustSummaryProvider = TrustSummaryProvider._();

/// The current user's trust standing (the restricted-state screens read this).

final class TrustSummaryProvider
    extends
        $FunctionalProvider<
          AsyncValue<TrustSummary>,
          TrustSummary,
          FutureOr<TrustSummary>
        >
    with $FutureModifier<TrustSummary>, $FutureProvider<TrustSummary> {
  /// The current user's trust standing (the restricted-state screens read this).
  TrustSummaryProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'trustSummaryProvider',
        isAutoDispose: true,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$trustSummaryHash();

  @$internal
  @override
  $FutureProviderElement<TrustSummary> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<TrustSummary> create(Ref ref) {
    return trustSummary(ref);
  }
}

String _$trustSummaryHash() => r'3a36b60ee7c25fef33425d5fa81260f45c64ce10';

/// The users the current user has blocked or muted.

@ProviderFor(myBlocks)
final myBlocksProvider = MyBlocksProvider._();

/// The users the current user has blocked or muted.

final class MyBlocksProvider
    extends
        $FunctionalProvider<
          AsyncValue<List<BlockEntry>>,
          List<BlockEntry>,
          FutureOr<List<BlockEntry>>
        >
    with $FutureModifier<List<BlockEntry>>, $FutureProvider<List<BlockEntry>> {
  /// The users the current user has blocked or muted.
  MyBlocksProvider._()
    : super(
        from: null,
        argument: null,
        retry: null,
        name: r'myBlocksProvider',
        isAutoDispose: true,
        dependencies: null,
        $allTransitiveDependencies: null,
      );

  @override
  String debugGetCreateSourceHash() => _$myBlocksHash();

  @$internal
  @override
  $FutureProviderElement<List<BlockEntry>> $createElement(
    $ProviderPointer pointer,
  ) => $FutureProviderElement(pointer);

  @override
  FutureOr<List<BlockEntry>> create(Ref ref) {
    return myBlocks(ref);
  }
}

String _$myBlocksHash() => r'e91803786a258cbae9150037e1b319921f5e2d76';
